"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.serializeToText = void 0;
const Slate = __importStar(require("slate"));
const guards_1 = require("../guards.cjs");
const nodeToString = (node, options) => {
    if (!Slate.Element.isElement(node))
        return Slate.Node.string(node);
    if ((0, guards_1.isLinkElement)(node) && options.markdownLinks)
        return `[${(0, exports.serializeToText)(node.children)}](${node.url})`;
    if ((0, guards_1.isVariableElement)(node) && options.encodeVariables)
        return `{${node.name}}`;
    return nodesToString(node.children, options).join('');
};
const nodesToString = (nodes, options) => nodes.map((node) => nodeToString(node, options));
const serializeToText = (content, { encodeVariables = true, markdownLinks = false } = {}) => nodesToString(content, { encodeVariables, markdownLinks }).join('\n').trim();
exports.serializeToText = serializeToText;
//# sourceMappingURL=text.cjs.map