const CAPITAL_LETTER_REGEXP = /[A-Z]/g;
export const cssPropertyNameToStyleName = (name) => name.replace(CAPITAL_LETTER_REGEXP, (match) => `-${match.toLowerCase()}`);
export const convertCSSPropertiesToStyles = (properties) => Object.entries(properties)
    .map(([key, value]) => `${cssPropertyNameToStyleName(key)}:${value}`)
    .join(';');
//# sourceMappingURL=styles.mjs.map