import { BaseText } from '@voiceflow/base-types';
export interface TextSerializerOptions {
    markdownLinks?: boolean;
    encodeVariables?: boolean;
}
export declare const serializeToText: (content: BaseText.SlateTextValue, { encodeVariables, markdownLinks }?: TextSerializerOptions) => string;
