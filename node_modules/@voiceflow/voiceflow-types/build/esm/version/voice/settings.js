import { VoiceVersion } from '@voiceflow/voice-types';
import { Voice } from "../../constants";
import { defaultBaseSettings } from '../base';
export const defaultSharedVoiceSettings = (settings = {}) => (Object.assign({}, VoiceVersion.defaultSettings(settings, { defaultPromptVoice: Voice.DEFAULT })));
export const defaultVoiceSettings = (settings = {}) => (Object.assign(Object.assign({}, defaultSharedVoiceSettings(settings)), defaultBaseSettings(settings)));
//# sourceMappingURL=settings.js.map