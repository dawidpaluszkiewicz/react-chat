var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { VoiceVersion } from '@voiceflow/voice-types';
import { Voice } from "../../constants";
import { defaultSharedVoiceSettings, defaultVoiceSettings } from './settings';
export * from './settings';
export const defaultSharedVoicePlatformData = (_a) => {
    var { settings } = _a, data = __rest(_a, ["settings"]);
    return (Object.assign(Object.assign({}, VoiceVersion.defaultPlatformData(data, { defaultPromptVoice: Voice.DEFAULT })), { settings: defaultSharedVoiceSettings(settings) }));
};
export const defaultVoicePlatformData = (_a) => {
    var { settings } = _a, data = __rest(_a, ["settings"]);
    return (Object.assign(Object.assign({}, defaultSharedVoicePlatformData(data)), { settings: defaultVoiceSettings(settings) }));
};
//# sourceMappingURL=index.js.map