import { VoiceVersion } from '@voiceflow/voice-types';
import { Voice } from "../../constants";
import { BaseSettings } from '../base';
export interface SharedVoiceSettings extends VoiceVersion.Settings<Voice> {
}
export interface VoiceSettings extends SharedVoiceSettings, BaseSettings {
}
export declare const defaultSharedVoiceSettings: (settings?: Partial<SharedVoiceSettings>) => SharedVoiceSettings;
export declare const defaultVoiceSettings: (settings?: Partial<VoiceSettings>) => VoiceSettings;
