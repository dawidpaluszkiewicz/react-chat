var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { ChatVersion } from '@voiceflow/chat-types';
import { defaultChatSettings, defaultSharedChatSettings } from './settings';
export * from './publishing';
export * from './settings';
export const defaultSharedChatPlatformData = (_a) => {
    var { settings } = _a, data = __rest(_a, ["settings"]);
    return (Object.assign(Object.assign({}, ChatVersion.defaultPlatformData(data)), { settings: defaultSharedChatSettings(settings) }));
};
export const defaultChatPlatformData = (_a) => {
    var { settings } = _a, data = __rest(_a, ["settings"]);
    return (Object.assign(Object.assign({}, defaultSharedChatPlatformData(data)), { settings: defaultChatSettings(settings) }));
};
//# sourceMappingURL=index.js.map