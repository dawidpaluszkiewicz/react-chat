import { ChatVersion } from '@voiceflow/chat-types';
import { defaultBaseSettings } from '../base';
export const defaultSharedChatSettings = (settings = {}) => (Object.assign({}, ChatVersion.defaultSettings(settings)));
export const defaultChatSettings = (settings = {}) => (Object.assign(Object.assign({}, defaultSharedChatSettings(settings)), defaultBaseSettings(settings)));
//# sourceMappingURL=settings.js.map