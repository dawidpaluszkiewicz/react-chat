export const isChatPrompt = (prompt) => !!(prompt && 'content' in prompt && Array.isArray(prompt.content));
export const isVoicePrompt = (prompt) => !!(prompt && 'content' in prompt && typeof prompt.content === 'string');
export const isIntentVoicePrompt = (prompt) => !!(prompt && 'text' in prompt && typeof prompt.text === 'string');
//# sourceMappingURL=prompt.js.map