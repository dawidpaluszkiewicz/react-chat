import { ProjectType } from "../constants";
import { defaultChatMemberPlatformData, defaultChatPlatformData } from './chat';
import { defaultVoiceMemberPlatformData, defaultVoicePlatformData } from './voice';
export * from './chat';
export * from './voice';
export const defaultPlatformData = (type, platformData) => {
    switch (type) {
        case ProjectType.CHAT:
            return defaultChatPlatformData(platformData);
        case ProjectType.VOICE:
            return defaultVoicePlatformData(platformData);
        default:
            throw new Error(`Unknown project type: ${type}`);
    }
};
export const defaultMemberPlatformData = (type, platformData) => {
    switch (type) {
        case ProjectType.CHAT:
            return defaultChatMemberPlatformData(platformData);
        case ProjectType.VOICE:
            return defaultVoiceMemberPlatformData(platformData);
        default:
            throw new Error(`Unknown project type: ${type}`);
    }
};
//# sourceMappingURL=index.js.map